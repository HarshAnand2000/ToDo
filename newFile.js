const { input, save } = require("./script");

input.addEventListener("keyup", () => {
  if (input.value.trim() == "") {
    // if(!save.classList.contains("disabled"){
    //      save.classList.add("opacity-50", "pointer-events-none", "disabled");
    // }
    if (save.classList.contains("disabled")) {
    }
  }
});
{
}
